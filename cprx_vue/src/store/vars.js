const state = {
    is_active: 'is-active'
}

const getters = {
    is_active: (state) => state.is_active
}

const actions = {
    
}

const mutations = {

}

export default {
    namespaced: true,

    state,
    getters,
    actions,
    mutations
}